import React from 'react';

interface IProps {
    postProject?: (action: any, action2: number) => void,
}

const ProjectOverview: React.SFC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export default ProjectOverview;